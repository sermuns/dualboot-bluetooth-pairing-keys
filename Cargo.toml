[package]
name = "dualboot-bt-link-keys"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4", features = ["derive"] }
color-eyre = "0.6"
nt-hive = "0.3.0"
uuid = { version = "1", default-features = false }
